{
  "name": "dsh-desktop",
  "version": "1.0.0",
  "type": "module",
  "description": "DeepSeek Harness Desktop (开箱即用桌面发行版，内置创作者工作台与精选插件)",
  "main": "src/main/index.js",
  "author": "JackAIStudio",
  "license": "MIT",
  "scripts": {
    "prepare-bundle": "node scripts/prepare-bundle.js",
    "dev": "node scripts/prepare-bundle.js && electron .",
    "test:sandbox": "node scripts/test-sandbox-run.js",
    "build:mac": "node scripts/prepare-bundle.js && electron-builder --mac -c.directories.output=${DSH_BUILD_OUT:-$HOME/.jds-build-out}",
    "build:mac:fast": "node scripts/prepare-bundle.js && electron-builder --mac --arm64 -c.directories.output=${DSH_BUILD_OUT:-$HOME/.jds-build-out}",
    "build:win": "node scripts/prepare-bundle.js && electron-builder --win -c.directories.output=${DSH_BUILD_OUT:-$HOME/.jds-build-out}",
    "build:all": "node scripts/prepare-bundle.js && electron-builder -mw -c.directories.output=${DSH_BUILD_OUT:-$HOME/.jds-build-out}"
  },
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.1-rc.2",
    "@deepseek-ai/dsh-web-app": "0.1.1-rc.2"
  },
  "devDependencies": {
    "electron": "^43.5.1",
    "electron-builder": "^25.1.8"
  }
}
