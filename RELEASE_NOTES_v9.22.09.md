## 🚀 JackDSH v9.22.09 发布说明

欢迎使用 JackDSH 开箱即用桌面客户端全新版本 **v9.22.09**！
这次把工作区的访达和终端入口修好了，图片附件能把真实路径交给模型，桌面版重启和退出也不会再留下抢端口的后台进程。

---

### ✨ 核心更新亮点

#### 1. 工作区可以在访达和终端打开
- `dsh-workspace-path` `0e44a36`：桌面版原版连接库挂不上工作区路由，点「在文件管理器中显示」和「在终端打开」会落到静态页，返回 HTTP 405。现在从已经拿到 webServer 的上下文注册路由。

#### 2. 图片附件带上可读取路径
- `dsh-grok-oauth` `4e71173`、`dsh-workbuddy-dual` `74eb14c`：给图片附件补上执行环境里的真实路径。旧版底座没有对应接口时，Grok 插件仍能加载。

#### 3. 桌面版重启留在应用进程里
- `dsh-web-restart` `535fd36`：桌面版不再自己脱离进程树拉起服务，交给 JackDSH 在原进程里重启。
- JackDSH `5a62fd1`：退出时等核心服务停掉；下次启动只清理这个数据目录里的孤儿进程，开发态 3080 不受影响。

#### 4. 构建缓存按版本隔离
- JackDSH `90dadac`：内置 bsk 的缓存路径带上版本号，避免换版本时复用旧的二进制。

---

### 📥 客户端下载

- **macOS (Apple Silicon arm64)**: `JackDSH-9.22.09-Mac-arm64.dmg`
- **Windows 绿色免安装便携版 (x64)**: `JackDSH-9.22.09-Windows-portable.zip`
